/**
 * Canonical URL prefix for the in-store manager UI (staff).
 * Legacy `/predajna/*` URLs redirect here (middleware, HTTP 308).
 */
export const STORE_MANAGER_BASE_PATH = "/manager/predajna" as const;
